{
  "name": "mongo-db-odata4",
  "version": "0.2.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "publish:contracts": "npm publish --workspace packages/odata-mongo-contracts --access public",
    "publish:core": "npm publish --workspace packages/odata-mongo-core --access public",
    "publish:express": "npm publish --workspace packages/odata-mongo-express --access public",
    "publish:fastify": "npm publish --workspace packages/odata-mongo-fastify --access public"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^20.12.12",
    "@types/supertest": "^6.0.3",
    "express": "^5.1.0",
    "ioredis": "^5.7.0",
    "ioredis-mock": "^8.9.0",
    "mongodb": "^6.18.0",
    "supertest": "^7.1.4",
    "typescript": "^5.5.4",
    "vitest": "^2.1.9"
  }
}
